# tokenhub 项目协作守则

本文档定义 tokenhub 项目的协作规范，适用于所有 AI Agent 和人类开发者。

**项目定位**：面向小白用户的 AI 教程门户网站，收录和展示各类 AI 工具的使用教程，帮助初学者快速上手 AI 应用。

---

## 协作守则（HARD RULES）

以下六条规则是本项目最高优先级的行为准则，任何 AI Agent 与人类开发者都必须遵守。

### 1. 需求不清不写码
- 任何不明确的问题，**必须**先提出并与用户沟通确认。
- 严禁在需求未澄清时擅自做假设并写代码 / 写文档结论。
- 出现歧义时，**停下询问**，而非猜测。
- 适用范围包含但不限于：功能边界、用户角色、数据形态、API 接口设计、验收标准。

### 2. 原子化提交 & 立即 push
- 每次变更必须是一个**原子化提交**：单一目的、可独立回滚、可独立 review。
- 完成提交后**立即 push** 到远端 `origin`。
- Commit message 写明 *为什么*（why），而非仅 *做了什么*。
- 不允许把多个无关变更合并到一个 commit。

### 3. 文档放置规范
- 根目录**只允许**以下文档：
  - `README.md` — 项目入口
  - `CLAUDE.md` — 本文件（协作守则）
  - 其他必要的根级配置/说明（如 `LICENSE`、`.gitignore`、`.env.example` 等）
- 其他**所有**项目文档必须放入 `docs/` 目录。
- `docs/` 必须维护目录索引（`docs/README.md`），新增/删除/重命名文档时同步更新索引。
- 文档之间需要建立**相互引用**（相对路径），方便 Agent 快速跳转检索。
- 文档目的：让任何 Agent 都能通过文档快速理解项目并接手开发，而不是依赖会话上下文。

### 4. 变更四步法
任何**需求变更或功能变更**必须按顺序执行，**顺序不能颠倒**：

1. **确认变更边界** — 影响哪些模块？涉及哪些文档？是否有兼容性 / 数据迁移问题？
2. **调整文档** — 先更新对应的需求 / 设计 / 接口 / 操作文档。
3. **修改代码** — 按更新后的文档实施。
4. **同步测试** — 补充或更新测试用例，确保覆盖变更点（含正常路径 + 边界路径）。

> 文档先行，代码后到，测试同步。

### 5. 会话续接 & 文档进度同步
- **新会话开始时**，必须先完成以下检查再进入开发：
  1. 阅读最新的 spec / plan，梳理上次会话完成了哪些任务、哪些未完成。
  2. 检查 `docs/` 下的文档是否与当前代码进度一致（plan 中的 checkbox、spec 中的状态标注、README 索引等）。
  3. 将梳理结果向用户汇报，**经用户确认后**再继续开发。
- **开发过程中**，每完成一个任务或阶段，**立即同步更新**对应文档（plan checkbox、进度标注、接口文档等），不得积压到最后批量更新。
- 文档进度与代码进度不一致时，**先补齐文档**再继续下一个任务。

### 6. 遵循最佳实践与开发文档
- 遵循行业最佳实践（语言规范、安全、可观测性、可访问性等）。
- 当 `docs/` 中存在具体的开发规范文档时，**必须遵循**。
- 行业最佳实践与项目文档冲突时，**以项目文档为准**，并在沟通 / PR 中显式提出讨论后再决定是否更新文档。

---

## 标准工作流程

```
需求梳理（Brainstorming）  ──►  docs/specs/<日期>-<主题>-design.md
        │
        ▼
方案设计（Design）           ──►  docs/specs/  (与上面同一类目录)
        │
        ▼
实施计划（Planning）         ──►  docs/plans/<日期>-<主题>-plan.md
        │
        ▼
开发与测试（Implementation） ──►  代码 + 测试（提交粒度遵循规则 2）
        │
        ▼
代码评审（Review & Merge）
```

每个阶段产物都应在 `docs/README.md` 中登记。

---

## 文档组织原则

### 渐进式披露
- `docs/README.md` 作为总索引，提供项目文档全景图
- 每个子目录有自己的 `README.md`，说明该目录的组织方式
- 文档通过引用链接，避免内容重复
- 从概览到细节，从共用 -> 专用，层层递进

### 文档分类
- **架构文档**：系统架构、技术选型、前后端分离方案
- **规范文档**：开发规范、代码风格、测试策略
- **设计文档**：功能需求设计、接口设计
- **计划文档**：实施计划、任务分解

### 文档与代码同步
- 完成一个功能时，通过文档确定修改范围
- 修改完成并通过测试验收后，**同步修改相关文档**
- 保证代码（注释）和文档是统一的
- 文档过时比没有文档更危险

---

## 多 Agent 协作规范

### 角色分离
- **主 Agent（Orchestrator）**：只负责任务调度、进度追踪、结果汇总。主 Agent **不写业务代码**。
- **开发 Agent（Worker）**：接收具体任务，完成代码编写与单元测试。每个 Worker 通过独立 worktree 工作，避免冲突。
- **审查 Agent（Reviewer）**：独立的 Code Review Agent，**不参与该任务的编码**。严禁自己写自己审。

### 并行开发
- 实施计划中标注"可并行"的任务，**应当**同时派发给多个 Worker Agent 并发执行。
- 每个并行 Worker 使用独立的 git worktree / branch 工作。
- 并行任务完成后，由主 Agent 协调合并顺序，处理冲突。

### Code Review 流程
1. Worker 完成任务并提交后，**必须**由独立的 Reviewer Agent 审查。
2. Reviewer 检查：正确性、安全性、是否符合项目规范（CLAUDE.md）、测试覆盖。
3. Reviewer 发现问题时，将修改意见返回给原 Worker 修复，而非自行修改。
4. Review 通过后，主 Agent 才能将变更合入主分支。

### 调度原则
- 主 Agent 在派发任务前，先确认任务间的依赖关系，只并行无依赖的任务。
- 每个 Worker 的任务描述必须自包含：包含相关文件路径、接口定义、依赖的类型/模块信息，不依赖会话上下文。
- Worker 完成后向主 Agent 报告：变更了哪些文件、测试是否通过、是否有遗留问题。

### 前后端分工
- **前端 Worker**：负责 `client/` 目录下的变更，关注 UI 交互、组件开发、状态管理、API 调用。
- **后端 Worker**：负责 `server/` 目录下的变更，关注数据模型、业务逻辑、API 路由、中间件。
- 涉及前后端联动的任务，需在 plan 中明确接口契约（请求/响应格式），前端可基于契约先 mock 开发，后端完成后联调。

---

## 给 Agent 的提示

- 接手本项目时，先读 `README.md` → `CLAUDE.md` → `docs/README.md` → 最新的 spec / plan。
- 遇到不明确处，**优先提问**，不要假设。
- 提交前确认是否符合规则 2（原子化）与规则 4（文档先行）。
- 任何看似"小"的便利性偏离规则的行为，都需要先沟通。
- 修改代码后，检查是否需要同步更新文档。
- 本项目为前后端分离架构，修改时注意区分 `client/` 和 `server/` 的职责边界。

---

## 技术栈

### 前端（`client/`）
- **框架**：React 18 + Vite 5
- **样式**：Tailwind CSS v3
- **动画**：Framer Motion
- **路由**：React Router v6
- **状态管理**：Zustand
- **Markdown**：react-md-editor + react-markdown + remark-gfm + rehype-highlight
- **HTTP**：Axios

### 后端（`server/`）
- **运行时**：Node.js 18+
- **框架**：Express 4
- **数据库**：MongoDB 6+ / MongoDB Memory Server（开发环境）
- **ODM**：Mongoose 8
- **认证**：JWT + bcryptjs
- **文件上传**：Multer

### 项目结构
```
tokenhub/
├── client/                  # React 前端（端口 3000）
│   └── src/
│       ├── components/      # UI 组件
│       │   ├── admin/       # 管理后台组件
│       │   ├── common/      # 通用组件
│       │   ├── home/        # 首页组件
│       │   └── tutorial/    # 教程组件
│       ├── pages/           # 页面组件
│       ├── services/        # API 封装
│       ├── store/           # Zustand 状态管理
│       └── hooks/           # 自定义 Hooks
├── server/                  # Express 后端（端口 5000）
│   └── src/
│       ├── controllers/     # 业务逻辑
│       ├── models/          # Mongoose 数据模型
│       ├── routes/          # 路由定义
│       ├── middleware/      # 中间件（认证/权限/上传）
│       ├── config/          # 配置（数据库连接等）
│       └── data/            # 种子数据
└── README.md
```

### 部署
- 前后端分离部署，后续部署到服务器
- 前端开发端口：3000，后端开发端口：5000
- 生产环境部署方案待定

详见 [`docs/architecture/tech-stack.md`](./docs/architecture/tech-stack.md)
